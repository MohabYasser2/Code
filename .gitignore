# --- Build artefacts generated by bison / flex / gcc ---
Compiler.exe
Parser.tab.c
Parser.tab.h
lex.yy.c
Parser.output

# --- Runtime outputs produced by the compiler ---
SymbolTable.out
SymbolTableVisualiser.out
SemanticAnalysis.out
Quadruples.out

# --- Local scratch / sample input (commit one canonical example if needed) ---
src.txt
err.txt
build.bat
build_log.txt

# --- Editor / OS noise ---
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db